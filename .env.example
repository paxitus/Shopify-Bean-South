# Copy to `.env` (gitignored) for local theme dev on a password-protected storefront.
# Shopify CLI reads these as flags (see `shopify theme dev --help`).
#
#   cp .env.example .env
#   # edit .env and set the real password
#
SHOPIFY_FLAG_STORE_PASSWORD=
